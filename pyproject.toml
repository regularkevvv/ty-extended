[project]
name = "ty-extended"
version = "0.73.0"
requires-python = ">=3.8"
dependencies = []
description = "A ty fork with semantic extension support."
readme = "README.md"
authors = [{ name = "Kevin Castro", email = "hola@kev.pe" }]
keywords = ["ty", "typing", "analysis", "check", "extensions"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Rust",
    "Topic :: Software Development :: Quality Assurance",
    "Topic :: Software Development :: Testing",
    "Topic :: Software Development :: Libraries",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.urls]
Repository = "https://github.com/regularkevvv/ty-extended"
Changelog = "https://github.com/regularkevvv/ty-extended/blob/main/CHANGELOG.md"
Releases = "https://github.com/regularkevvv/ty-extended/releases"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[dependency-groups]
dev = ["prek==0.4.11"]
release = ["rooster"]
docs = [
    "mdformat>=0.7.17",
    "mdformat-admon>=2.0.2",
    "mdformat-mkdocs>=2.0.4",
    "mkdocs>=1.5.0",
    "mkdocs-git-revision-date-localized-plugin>=1.3.0",
    "mkdocs-llmstxt>=0.2.0",
    "mkdocs-material>=9.7.0",
    "mkdocs-redirects>=1.2.2",
]

[tool.uv]
no-build = true
exclude-newer = "P7D"
cache-keys = [
    { file = "pyproject.toml" },
    { file = "dist-workspace.toml" },
    { file = "ruff/Cargo.toml" },
    { file = "ruff/Cargo.lock" },
    { file = "**/*.rs" },
]

[tool.uv.dependency-groups]
dev = {requires-python = ">=3.12"}
release = {requires-python = ">=3.12"}
docs = {requires-python = ">=3.12"}

[tool.ruff]
extend-exclude = ["ruff"]
per-file-target-version = { "scripts/**" = "py313" }

[tool.ruff.lint]
select = [
    "E",   # pycodestyle (error)
    "F",   # pyflakes
    "B",   # bugbear
    "B9",
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
    "I",   # isort
    "UP",  # pyupgrade
    "PIE", # flake8-pie
    "PGH", # pygrep-hooks
    "PYI", # flake8-pyi
    "RUF",
]

ignore = [
    # only relevant if you run a script with `python -0`,
    # which seems unlikely for any of the scripts in this repo
    "B011",
    # Leave it to the formatter to split long lines and
    # the judgement of all of us.
    "E501",
]

[tool.ruff.lint.isort]
required-imports = ["from __future__ import annotations"]

[tool.maturin]
bindings = "bin"
manifest-path = "ruff/crates/ty/Cargo.toml"
module-name = "ty"
python-source = "python"
strip = true
features = ["plugins-wasm"]
include = [
    { path = "dist-workspace.toml", format = [
        "sdist",
    ] },
    { path = "LICENSE", format = "sdist" },
]

[tool.rooster]
version-files = [
    "pyproject.toml",
    { path = "dist-workspace.toml", field = "workspace.version", format = "cargo" },
    "docs/installation.md",
]
submodules = ["ruff"]
require-labels = [{ submodule = "ruff", labels = ["ty"] }]
ignore-labels = [
    "internal",
    "testing",
    "ci",
    "playground",
    "documentation",
    "release",
]
changelog-ignore-authors = ["github-actions"]
major-labels = [] # We do not use the major version number yet
minor-labels = [] # We do not use the minor version number yet
version-format = "cargo"
trim-title-prefixes = ["[ty]"]

[tool.rooster.section-labels]
"Breaking changes" = ["breaking"]
"Preview features" = ["preview"]
"Bug fixes" = ["bug"]
"LSP server" = ["server"]
"CLI" = ["cli"]
"Diagnostics" = ["diagnostics"]
"Configuration" = ["configuration"]
